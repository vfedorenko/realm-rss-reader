<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="channel"
            type="com.weezlabs.realmexample.presentation.channelsmodule.viewmodels.ChannelViewModel"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="@dimen/activity_vertical_margin"
        tools:context=".presentation.channelsmodule.activities.CreateChannelActivity">

        <android.support.design.widget.TextInputLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <EditText
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/title"
                android:inputType="text"
                android:maxLines="1"
                android:singleLine="true"
                android:afterTextChanged="@{channel.onTitleChanged}" />

        </android.support.design.widget.TextInputLayout>

        <android.support.design.widget.TextInputLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <EditText
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/link"
                android:imeActionId="@+id/login"
                android:imeActionLabel="@string/done"
                android:imeOptions="actionDone"
                android:inputType="textUri"
                android:maxLines="1"
                android:singleLine="true"
                android:afterTextChanged="@{channel.onLinkChanged}"
                android:onEditorAction="@{channel.onDoneClick}"/>

        </android.support.design.widget.TextInputLayout>

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/done"
            android:onClick="@{channel.onDoneClick}"/>

    </LinearLayout>
</layout>
